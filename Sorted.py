def sorted_filter(number):
    number = sorted(numbers_digits)
    print(number)





numbers_string = input().split()
numbers_digits = []

for number in numbers_string:
    numbers_digits.append(int(number))



sorted_filter(numbers_digits)